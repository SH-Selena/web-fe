## 冒泡
遍历，找到最小的，交换
## 插入
每次排一个元素，找到对应位置插入
## 归并 FireFox sort()
将小数组归并成大数组
## 快速排序 Google V8 sort()
选中间的数为基准，小于基准的在左，大于基准的在右；对子集再取基准，直至子集只有一个元素
## 计数排序

## 希尔排序

## 选择排序

## 堆排序

## 桶排序

## 不稳定的算法
快排、希尔排序、选择排序、堆排序