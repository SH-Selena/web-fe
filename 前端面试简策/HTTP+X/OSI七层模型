## OSI　七层模型（无应用）
应用层：为应用程序提供交互服务（域名系统DNS，万维网应用HTTP，电子邮件SMTP）。
表示层：数据格式转换（加密解密，转换翻译，压缩解压缩）。
会话层：在网络中的两节点之间建立维持和终止通信（服务器验证用户登录）。
运输层：向主机提供通用的数据传输服务（TCP、UDP）。
网络层：选择合适的路由和交换节点，确保数据及时传送（IP协议）。
数据链路层：将网络层传下来的ID 数据包装成帧，并在相邻节点的链路上传送帧。
物理层：实现相邻节点间比特流的透明传输，尽可能屏蔽传输介质和通信手段的差异。
## TCP/IP 四层模型（应用）

## 五层模型（教学）