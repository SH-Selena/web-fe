## HTTP
明文传输，端口为80，扩展性强，速度快，跨平台支持
1.0：无状态无连接，每次都建立 TCP 连接，队头阻塞，每次传输全部数据；
2.0：多路复用，并行传输，在一个 TCP 上进行任意数量的 HTTP 请求，二进制分帧；
3.0：基于 Google 的 QUIC 协议，减少 TCP 三次握手时间及 TLS 握手时间，流量控制；

## HTTPs
加密传输，端口 443，SSL/TLS，非对称加密