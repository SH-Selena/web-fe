箭头函数没有 this，函数内部有 this只取决于外面的第一个不是箭头函数的函数this。
this的指向在函数创建的时候是决定不了的，在调用的时候才能决定，谁调用的就指向谁。
函数中有 this -> 1. 未调用指向 window；2. 调用指向上一级的对象
如果返回的是一个对象，则 this 指向的是返回的对象，否则是函数实例。
new 关键词可以改变 this 的指向，因为 new 会创建一个空对象，然后调用将this指向空对象。
.call(要添加的环境, 1, 2)、.apply(..., [10, 1])立即执行
.bind(...)返回的是一个修改过后的函数，什么时候调用都可